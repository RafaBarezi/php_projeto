#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <stdbool.h> // trabalha com função booleana

/*

Nome do programa: Pesquisa Vetor.
Objetivo: Solicitar que o usuário digite a posição do vetor desejado.
Data Criação: 02/04/2022.

*/

int main()
{
    setlocale(LC_ALL, "Portuguese_Brazil");
    system("title Pesquisa Vetor");
    system("color 03");


    int vetor[] = {1, 3, 5, 7, 9};
    int num;
    bool achou = false

    printf("Qual número deseja procurar?");
    scanf("%i", &vetor[num]);

    for(num = 0 ; num < 5; num++)
    {

        if(vetor == num)
        {
            printf("Vetor encontrado na posição %i", posicao);

                    	}
    }

        if(vetor != num)
        {
            printf("Vetor não encontrado em nehuma posição");
        }

    return 0;
}
